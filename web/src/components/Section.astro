---
interface Props {
  id?: string;
  class?: string;
}
const { id = '', class: cls = '' } = Astro.props as Props;
---

<section id={id} class={`section ${cls}`}>
  <div class="container">
    <slot />
  </div>
</section>
